# DEPRECATED — Canonical location moved

The canonical `PROJECT-RULES.md` now lives at `docs/project-mode/PROJECT-RULES.md`.

The runtime symlink at `~/.openclaw/projects/PROJECT-RULES.md` is updated by migration m005
to point to the new canonical location.

This file is kept only for backwards compatibility reference.
