{
  "id": "flowboard",
  "name": "FlowBoard",
  "version": "5.2.1",
  "description": "Project workspaces, dashboard, and project-context hook for OpenClaw agents.",
  "enabledByDefault": false,
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "projectsDir": {
        "type": "string",
        "minLength": 1,
        "description": "Optional override for FLOWBOARD_PROJECTS_DIR."
      },
      "dashboardPort": {
        "type": "number",
        "minimum": 1,
        "maximum": 65535,
        "description": "Optional dashboard port the project-context hook uses when dashboardBaseUrl is not set."
      },
      "dashboardBaseUrl": {
        "type": "string",
        "minLength": 1,
        "description": "Optional dashboard API base URL for the project-context hook, for example http://localhost:18843."
      },
      "serviceToken": {
        "type": "string",
        "minLength": 32,
        "description": "Service credential shared with the dashboard (FLOWBOARD_SERVICE_TOKEN). Lets the Gateway act for the signed-in operator; store it as a SecretRef, never inline."
      }
    }
  },
  "uiHints": {
    "projectsDir": {
      "label": "Projects Directory",
      "help": "Optional path for FlowBoard project files. Prefer the FLOWBOARD_PROJECTS_DIR environment variable for service deployments.",
      "advanced": true
    },
    "dashboardPort": {
      "label": "Dashboard Port",
      "help": "Optional local dashboard port for the project-context hook when the service does not run on 18790.",
      "advanced": true
    },
    "dashboardBaseUrl": {
      "label": "Dashboard Base URL",
      "help": "Optional full base URL for the project-context hook. Prefer this for proxy, tunnel, or non-localhost setups.",
      "advanced": true
    },
    "serviceToken": {
      "label": "Service Token",
      "help": "Shared credential the Gateway sends to the dashboard (FLOWBOARD_SERVICE_TOKEN, at least 32 characters). Store it as a SecretRef; it lets the Gateway act for the signed-in operator on loopback.",
      "advanced": true,
      "secret": true
    }
  },
  "configContracts": {
    "secretInputs": {
      "bundledDefaultEnabled": false,
      "paths": [
        {
          "path": "serviceToken",
          "expected": "string"
        }
      ]
    }
  },
  "controlUi": {
    "entry": "dist/control-ui/8defc89712f950fe7c4c51ec74f8fd8f4c4165f222a6f699d26ba8a06a3231a4/index.js",
    "styles": [
      "dist/control-ui/8defc89712f950fe7c4c51ec74f8fd8f4c4165f222a6f699d26ba8a06a3231a4/index.css"
    ]
  },
  "contracts": {
    "tools": []
  }
}
